[request_definition]
r = sub, db, obj, act

[policy_definition]
p = sub, db, obj, act

[role_definition]
g = _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = g(r.sub, p.sub) && (r.db == p.db || p.db == "*") && (r.obj == p.obj || p.obj == "*") && (r.act == p.act || p.act == "*")
